// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Pin an upstream SSH host public key for a Formal resource. Multiple pins may exist for one resourceId when the host presents multiple key types.
 */
export class ResourceSshHostKey extends pulumi.CustomResource {
    /**
     * Get an existing ResourceSshHostKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ResourceSshHostKeyState, opts?: pulumi.CustomResourceOptions): ResourceSshHostKey {
        return new ResourceSshHostKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'formal:index/resourceSshHostKey:ResourceSshHostKey';

    /**
     * Returns true if the given object is an instance of ResourceSshHostKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ResourceSshHostKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ResourceSshHostKey.__pulumiType;
    }

    /**
     * OpenSSH public key of the upstream SSH host (for example the output of `ssh-keygen -yf /etc/ssh/ssh_host_ed25519_key`).
     */
    declare public readonly publicKey: pulumi.Output<string>;
    /**
     * Resource ID for which the SSH host key is pinned.
     */
    declare public readonly resourceId: pulumi.Output<string>;

    /**
     * Create a ResourceSshHostKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ResourceSshHostKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ResourceSshHostKeyArgs | ResourceSshHostKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ResourceSshHostKeyState | undefined;
            resourceInputs["publicKey"] = state?.publicKey;
            resourceInputs["resourceId"] = state?.resourceId;
        } else {
            const args = argsOrState as ResourceSshHostKeyArgs | undefined;
            if (args?.publicKey === undefined && !opts.urn) {
                throw new Error("Missing required property 'publicKey'");
            }
            if (args?.resourceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'resourceId'");
            }
            resourceInputs["publicKey"] = args?.publicKey;
            resourceInputs["resourceId"] = args?.resourceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ResourceSshHostKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ResourceSshHostKey resources.
 */
export interface ResourceSshHostKeyState {
    /**
     * OpenSSH public key of the upstream SSH host (for example the output of `ssh-keygen -yf /etc/ssh/ssh_host_ed25519_key`).
     */
    publicKey?: pulumi.Input<string | undefined>;
    /**
     * Resource ID for which the SSH host key is pinned.
     */
    resourceId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ResourceSshHostKey resource.
 */
export interface ResourceSshHostKeyArgs {
    /**
     * OpenSSH public key of the upstream SSH host (for example the output of `ssh-keygen -yf /etc/ssh/ssh_host_ed25519_key`).
     */
    publicKey: pulumi.Input<string>;
    /**
     * Resource ID for which the SSH host key is pinned.
     */
    resourceId: pulumi.Input<string>;
}
