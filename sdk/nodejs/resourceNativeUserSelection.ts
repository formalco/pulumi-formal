// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Controls which Native User V3 a session connects to a Resource as using a CEL expression. Enable Native Users V3 on the Resource separately with `nativeUsersV3Enabled`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as formal from "@formalco/pulumi";
 *
 * const db = new formal.Resource("db", {
 *     name: "analytics-postgres",
 *     hostname: "analytics.internal",
 *     technology: "postgres",
 *     port: 5432,
 *     nativeUsersV3Enabled: true,
 * });
 * const admin = new formal.NativeUserV3("admin", {
 *     basic: {
 *         password: {
 *             environmentVariable: "ANALYTICS_ADMIN_PASSWORD",
 *         },
 *         username: "app_admin",
 *     },
 *     resourceId: db.id,
 *     label: "admin",
 * });
 * const readOnly = new formal.NativeUserV3("read_only", {
 *     basic: {
 *         password: {
 *             environmentVariable: "ANALYTICS_READONLY_PASSWORD",
 *         },
 *         username: "app_readonly",
 *     },
 *     resourceId: db.id,
 *     label: "read-only",
 * });
 * // Sessions from the admins group connect as app_admin; everyone else connects as
 * // app_readonly.
 * const dbResourceNativeUserSelection = new formal.ResourceNativeUserSelection("db", {
 *     resourceId: db.id,
 *     cel: pulumi.interpolate`\"admins\" in user.groups ? \"${admin.id}\"
 * : \"${readOnly.id}\"
 * `,
 * });
 * ```
 */
export class ResourceNativeUserSelection extends pulumi.CustomResource {
    /**
     * Get an existing ResourceNativeUserSelection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ResourceNativeUserSelectionState, opts?: pulumi.CustomResourceOptions): ResourceNativeUserSelection {
        return new ResourceNativeUserSelection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'formal:index/resourceNativeUserSelection:ResourceNativeUserSelection';

    /**
     * Returns true if the given object is an instance of ResourceNativeUserSelection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ResourceNativeUserSelection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ResourceNativeUserSelection.__pulumiType;
    }

    /**
     * The CEL expression that returns the ID of the Native User a session connects as, or an empty string to refuse the session.
     */
    declare public readonly cel: pulumi.Output<string>;
    /**
     * The ID of the Resource this selection applies to. Only one selection can exist per Resource.
     */
    declare public readonly resourceId: pulumi.Output<string>;

    /**
     * Create a ResourceNativeUserSelection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ResourceNativeUserSelectionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ResourceNativeUserSelectionArgs | ResourceNativeUserSelectionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ResourceNativeUserSelectionState | undefined;
            resourceInputs["cel"] = state?.cel;
            resourceInputs["resourceId"] = state?.resourceId;
        } else {
            const args = argsOrState as ResourceNativeUserSelectionArgs | undefined;
            if (args?.cel === undefined && !opts.urn) {
                throw new Error("Missing required property 'cel'");
            }
            if (args?.resourceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'resourceId'");
            }
            resourceInputs["cel"] = args?.cel;
            resourceInputs["resourceId"] = args?.resourceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ResourceNativeUserSelection.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ResourceNativeUserSelection resources.
 */
export interface ResourceNativeUserSelectionState {
    /**
     * The CEL expression that returns the ID of the Native User a session connects as, or an empty string to refuse the session.
     */
    cel?: pulumi.Input<string | undefined>;
    /**
     * The ID of the Resource this selection applies to. Only one selection can exist per Resource.
     */
    resourceId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ResourceNativeUserSelection resource.
 */
export interface ResourceNativeUserSelectionArgs {
    /**
     * The CEL expression that returns the ID of the Native User a session connects as, or an empty string to refuse the session.
     */
    cel: pulumi.Input<string>;
    /**
     * The ID of the Resource this selection applies to. Only one selection can exist per Resource.
     */
    resourceId: pulumi.Input<string>;
}
