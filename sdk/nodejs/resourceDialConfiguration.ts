// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Creating a Dial Configuration of a Resource in Formal.
 */
export class ResourceDialConfiguration extends pulumi.CustomResource {
    /**
     * Get an existing ResourceDialConfiguration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ResourceDialConfigurationState, opts?: pulumi.CustomResourceOptions): ResourceDialConfiguration {
        return new ResourceDialConfiguration(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'formal:index/resourceDialConfiguration:ResourceDialConfiguration';

    /**
     * Returns true if the given object is an instance of ResourceDialConfiguration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ResourceDialConfiguration {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ResourceDialConfiguration.__pulumiType;
    }

    /**
     * How the connector dials this resource's upstream. Supported values: `tcp` (direct TCP via the resource's hostname and port), `gcpCloudsql` (dial via the GCP Cloud SQL connector library; set `dialTarget` to the `project:region:instance` connection name), `awsSsm` (dial via AWS Systems Manager; set `dialTarget` to the target instance or cluster ARN).
     */
    declare public readonly dialMethod: pulumi.Output<string>;
    /**
     * Method-specific dial target. For `gcpCloudsql`, the `project:region:instance` connection name. For `awsSsm`, the target instance or cluster ARN. Leave empty for `tcp`.
     */
    declare public readonly dialTarget: pulumi.Output<string | undefined>;
    /**
     * Resource ID for which the dial configuration is applied to.
     */
    declare public readonly resourceId: pulumi.Output<string>;

    /**
     * Create a ResourceDialConfiguration resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ResourceDialConfigurationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ResourceDialConfigurationArgs | ResourceDialConfigurationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ResourceDialConfigurationState | undefined;
            resourceInputs["dialMethod"] = state?.dialMethod;
            resourceInputs["dialTarget"] = state?.dialTarget;
            resourceInputs["resourceId"] = state?.resourceId;
        } else {
            const args = argsOrState as ResourceDialConfigurationArgs | undefined;
            if (args?.dialMethod === undefined && !opts.urn) {
                throw new Error("Missing required property 'dialMethod'");
            }
            if (args?.resourceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'resourceId'");
            }
            resourceInputs["dialMethod"] = args?.dialMethod;
            resourceInputs["dialTarget"] = args?.dialTarget;
            resourceInputs["resourceId"] = args?.resourceId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ResourceDialConfiguration.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ResourceDialConfiguration resources.
 */
export interface ResourceDialConfigurationState {
    /**
     * How the connector dials this resource's upstream. Supported values: `tcp` (direct TCP via the resource's hostname and port), `gcpCloudsql` (dial via the GCP Cloud SQL connector library; set `dialTarget` to the `project:region:instance` connection name), `awsSsm` (dial via AWS Systems Manager; set `dialTarget` to the target instance or cluster ARN).
     */
    dialMethod?: pulumi.Input<string | undefined>;
    /**
     * Method-specific dial target. For `gcpCloudsql`, the `project:region:instance` connection name. For `awsSsm`, the target instance or cluster ARN. Leave empty for `tcp`.
     */
    dialTarget?: pulumi.Input<string | undefined>;
    /**
     * Resource ID for which the dial configuration is applied to.
     */
    resourceId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ResourceDialConfiguration resource.
 */
export interface ResourceDialConfigurationArgs {
    /**
     * How the connector dials this resource's upstream. Supported values: `tcp` (direct TCP via the resource's hostname and port), `gcpCloudsql` (dial via the GCP Cloud SQL connector library; set `dialTarget` to the `project:region:instance` connection name), `awsSsm` (dial via AWS Systems Manager; set `dialTarget` to the target instance or cluster ARN).
     */
    dialMethod: pulumi.Input<string>;
    /**
     * Method-specific dial target. For `gcpCloudsql`, the `project:region:instance` connection name. For `awsSsm`, the target instance or cluster ARN. Leave empty for `tcp`.
     */
    dialTarget?: pulumi.Input<string | undefined>;
    /**
     * Resource ID for which the dial configuration is applied to.
     */
    resourceId: pulumi.Input<string>;
}
